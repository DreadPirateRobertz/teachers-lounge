apiVersion: v1
kind: Service
metadata:
  name: tutoring-service
  namespace: teachers-lounge
  labels:
    app: tutoring-service
spec:
  type: ClusterIP
  selector:
    app: tutoring-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
