apiVersion: v1
kind: Service
metadata:
  name: ai-gateway
  namespace: teachers-lounge
  labels:
    app: ai-gateway
spec:
  type: ClusterIP
  selector:
    app: ai-gateway
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
