# Development Dockerfile — hot reload via Air
FROM golang:1.23-alpine
WORKDIR /app
RUN apk add --no-cache git && \
    go install github.com/air-verse/air@latest
COPY go.mod go.sum ./
RUN go mod download
# Source is bind-mounted at runtime
EXPOSE 8080
CMD ["air", "-c", ".air.toml"]
