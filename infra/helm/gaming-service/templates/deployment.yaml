apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gaming-service.fullname" . }}
  labels:
    {{- include "gaming-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "gaming-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gaming-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: gaming-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            # Secrets from Kubernetes Secret "gaming-service-secrets"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gaming-service-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: gaming-service-secrets
                  key: jwt-secret
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
